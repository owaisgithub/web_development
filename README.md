# Web Development Project:
This is great project.I have add lots of functionality to it 
This is a ecommerce website.

### You can start this project, the step are given below
### Step 1:
First of all create virtual environment
Install virtualenvwrapper
`pip install virtualenvwrapper-win `
Now create virtual environment
`mkvirtualenv 'your-enviroment-name'`

### Step 2:
To create environment.Let's jump into your environment
Let's type following command
`workon 'your-environment-name`
Now intall django 
`pip install django`

### Step 3:
Intall following packages before run the project
Python Imaging Library
`pip insatll Pillow`


